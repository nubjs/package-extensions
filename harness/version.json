{
  "version": "1.0.0",
  "policy": "Plain semver, matching @yarnpkg/extensions. Patch = a data refresh, bumped and committed automatically by release.yml when a scheduled run finds the packageExtensions rules have moved. Minor/major = a change in the exported shape, which is what a consumer pinning this package actually needs to know about; edit this file by hand for those and dispatch release.yml."
}
